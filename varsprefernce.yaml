- name : checking the vars prefernce
  hosts: webservers
  vars:
    last_Name: "Sushma"
  #vars_files:
    #- course.yaml

  #vars_prompt:
    #- name: "last_Name"
    #  prompt: "Please enter your name"
    #  private: no
    
  tasks:
    - name: Print my name
      #vars:
        #last_Name: "John Doe"
      debug:
        msg: "hi {{ last_Name }}"

# CMD
# Task vars
# Var_files
# Vars_prompt
# playbook vars
# Inventory vars